{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"description": "Comment describing your JSON Schema",
	"type": "object",
	"additionalProperties": false,
	"properties": {
		"id": {
			"description": "The id of the object on which you would like to perform the action",
			"type": "string"
		},
		"index_name": {
            "description": "The name of the index you would like to retrieve",
            "type": "string"
        },
        "unique": {
            "type": "boolean",
            "description": "If true, only unique indexes with unique will be returned"
        }
	}
}